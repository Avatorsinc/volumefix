Volumefix is an app to lock out volume settings from users
app can be used mainly in enterprise solutions

use app-debug.apk to install it, 
for sotimobicontrol use mcstudio.exe to create package with it. 

